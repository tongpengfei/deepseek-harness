/** Host loader entry for the Apps catalog browser implementation exported from `./client`. */

/** Host plugin body — scenario Apps run entirely in the browser client. */
export function apply(): void {}

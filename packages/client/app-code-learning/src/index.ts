/** Host loader entry for the browser-only programming course exported from `./client`. */

/** Host plugin body — course presentation and progress remain in the browser client. */
export function apply(): void {}
